import { Home } from '../pages';
import { HomeFilled } from '@ant-design/icons';

export default [
    {
        path: '/home',
        component: Home,
        name: 'Home',
        icon: HomeFilled,
    },
];
