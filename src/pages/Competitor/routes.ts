import Competitor from './Competitor';
import constantRoutes from '@constants/routes';

const routes = [
    {
        path: constantRoutes.ROUTES.COMPETITOR.ROUTE,
        component: Competitor,
    },
];

export default routes;
