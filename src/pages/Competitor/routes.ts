import Competitor from './Competitor';
import constants from '@constants';

const routes = [
    {
        path: constants.ROUTES.COMPETITOR.ROUTE,
        component: Competitor,
    },
];

export default routes;
