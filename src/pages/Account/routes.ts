import Account from './Account';
import constantRoutes from '@constants/routes';

const routes = [
    {
        path: constantRoutes.ROUTES.ACCOUNT.ROUTE,
        component: Account,
    },
];

export default routes;
