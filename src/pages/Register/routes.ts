import Register from './Register';
import constantRoutes from '@constants/routes';

const routes = [
    {
        path: constantRoutes.ROUTES.REGISTER.ROUTE,
        component: Register,
    },
];

export default routes;
