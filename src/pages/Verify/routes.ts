import Verify from './Verify';
import constantRoutes from '@constants/routes';

const routes = [
    {
        path: constantRoutes.ROUTES.VERIFY.ROUTE,
        component: Verify,
    },
];

export default routes;
