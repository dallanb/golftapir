import ContestMatchup from './ContestMatchup';
import constants from '@constants';

const routes = [
    {
        path: constants.ROUTES.CONTEST_MATCHUP,
        component: ContestMatchup,
    },
];

export default routes;
