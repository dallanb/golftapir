import { PropsWithChildren } from 'react';
import { ListChildComponentProps } from 'react-window';

export interface CompetitorsListTileProps {
    props: PropsWithChildren<ListChildComponentProps>;
    history: any;
}
