import League from './League';
import constantRoutes from '@constants/routes';

const routes = [
    {
        path: constantRoutes.LEAGUE_APP.LEAGUE.ROUTE,
        component: League,
    },
];

export default routes;
