import React from 'react';
import SiderLayoutContent from '@layouts/SiderLayout/SiderLayoutContent';
import { LeagueSiderContentProps } from './types';

const LeagueSiderContent: React.FunctionComponent<LeagueSiderContentProps> = ({}) => {
    return (
        <SiderLayoutContent>
            <div />
        </SiderLayoutContent>
    );
};

export default LeagueSiderContent;
