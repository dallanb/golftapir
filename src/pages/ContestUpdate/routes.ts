import ContestUpdate from './ContestUpdate';
import constantRoutes from '@constants/routes';

const routes = [
    {
        path: constantRoutes.MEMBER_APP.CONTEST_UPDATE.ROUTE,
        component: ContestUpdate,
    },
];

export default routes;
