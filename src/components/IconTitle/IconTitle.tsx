import React from 'react';
import { IconTitleProps } from './types';

class IconTitle extends React.PureComponent<IconTitleProps> {
    render() {
        return <div />;
    }
}

export default IconTitle;
